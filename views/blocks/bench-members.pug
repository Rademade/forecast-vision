mixin benchMembers(reports)
    each report in reports
        td.week-info.rotation
            div Available members (>8 h.)
            div Total – #{report.getMembersList().getBenchDuration()} h.
            ul
                each member in report.getMembersList().getBenchMembers()
                    li #{member.getName()} – #{member.getBenchDuration()} h.
